{% set hostname = salt['mine.get']('G@stack_id:' ~ grains.stack_id ~ ' and G@roles:monitor.sensu.server', 'grains.items', 'compound').values()[0]['fqdn'] %}
{
  "api": {
    "host": "{{hostname}}",
    "port": 4567,
    "user": "{{ pillar.monitor.sensu.api.username}}",
    "password": "{{ pillar.monitor.sensu.api.password }}"
  },
  "redis": {
    "host": "{{hostname}}",
    "port": 6379
  }
}
