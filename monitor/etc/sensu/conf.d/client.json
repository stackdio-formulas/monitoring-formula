{%- set hostname = salt['mine.get']('G@fqdn:' ~ grains.fqdn, 'grains.items', 'compound').values()[0]['fqdn'] -%}
{
  "client": {
    "name": "{{ hostname }}",
    "address": "{{ hostname }}",
    "subscriptions": {{ pillar.monitor.sensu.client.subscriptions | json }}
  }
}
